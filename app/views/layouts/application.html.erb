<!DOCTYPE html>
<html>
  <head>
    <title>Schedule</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div id='container'>
      <div id='header'>
        Really Good Pizza              <!-- this needs to be a variable -->
      </div>
      <%= render "layouts/menu" %>
      <div id='yield'>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
